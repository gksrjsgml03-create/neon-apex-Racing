# NEON APEX V1.5 · 친구와 함께 달리기

싱글플레이는 서버 없이 계속 이용할 수 있습니다. 멀티플레이는 2~8명, 3랩이며 AI가 들어오지 않습니다. 방 코드와 서버 주소가 들어 있는 초대 정보로 참가합니다.

## 같은 공유기 / 같은 Wi-Fi에서

1. 모두 같은 V1.5 게임 폴더를 가지고 `NeonApex.exe`를 실행합니다.
2. 방장은 **멀티플레이 → 닉네임 입력 → 내 PC에서 방 열기**를 누릅니다. 별도 Node.js 설치 없이 서버가 게임 안에서 시작됩니다.
3. 대기실에서 맵을 고르고 **초대 복사**로 복사한 정보를 친구에게 보냅니다.
4. 친구는 **멀티플레이 → 초대 정보 붙여넣기 → 참가하기**를 누릅니다.
5. 친구들이 **준비 완료**를 누르면 방장의 **레이스 시작**이 활성화됩니다.

Windows 방화벽이 물으면 사설 네트워크에서 게임 통신을 허용해야 합니다. 자동으로 방화벽이나 공유기 설정을 변경하지 않습니다. Wi-Fi의 기기 간 통신 차단 기능이 켜져 있으면 접속할 수 없습니다.

VPN/가상 어댑터 때문에 초대 주소가 잘못 선택되면 대기실의 **초대 주소 설정**에서 같은 공유기에 연결된 PC의 IPv4 주소로 바꾸세요. 예: `ws://192.168.0.10:8787`. `127.0.0.1`은 자기 PC를 뜻하므로 다른 PC에 보내면 안 됩니다.

서버를 실행한 PC는 게임을 종료하지 않아야 합니다. 방장 권한은 다른 참가자에게 넘어갈 수 있지만, 서버 자체를 다른 PC로 옮기는 기능은 아닙니다. 방장이 방을 나가도 앱을 열어 두면 그 PC의 서버는 계속 실행됩니다.

## 서로 다른 인터넷에서

현재 공개 서버와 도메인은 개설하지 않았습니다. 아래 배포 구성을 준비해 두었습니다. 인터넷 플레이는 실제 공개 서버를 실행한 후 이용할 수 있습니다.

공개 서버 주소가 준비되면 모두 **게임 서버 주소**에 `wss://서버도메인`을 입력합니다. 방장은 **입력한 서버에 방 만들기**를 누릅니다. 이후 초대와 준비 과정은 같습니다. 초대 정보에는 서버 주소도 들어 있어 친구가 따로 주소를 입력할 필요가 없습니다.

집 PC를 직접 공개하는 경우에는 공유기의 TCP 8787 포트 연결, Windows 방화벽 허용, 공인 IP가 필요합니다. 통신사의 CGNAT 환경에서는 포트 연결만으로 접근되지 않을 수 있습니다. 이 프로젝트는 자동 포트 개방이나 중계 서비스에 연결하지 않습니다.

## 공개 서버 배포 구성

서버는 Node.js 24와 WebSocket을 사용하며 별도 그래픽 환경이 필요 없습니다. 게임 상태를 메모리에 보관하므로 서버를 재시작하면 진행 중인 방은 사라집니다.

### Node.js로 실행

```sh
npm ci --omit=dev --ignore-scripts
npm run start:server
```

기본 주소는 `0.0.0.0:8787`입니다. 호스팅 환경에서 `PORT`와 필요하면 `HOST` 환경 변수로 바꿀 수 있습니다. 상태 확인은 `GET /health`이며 JSON `{ "ok": true, ... }`을 반환합니다. 게임 전용 서버는 소스 파일이나 웹페이지를 제공하지 않습니다.

### Docker로 실행

```sh
docker compose up -d --build
```

`Dockerfile`, `.dockerignore`, `compose.yaml`을 포함했습니다. 컨테이너 내부 8787 포트를 외부에 연결합니다. 도메인 기반 공개 배포에서는 TLS를 종료하는 프록시가 WebSocket 업그레이드를 지원하고 이 포트로 전달하도록 설정합니다. 방이 서버 메모리에 있으므로 **서버 인스턴스는 1개**로 실행합니다. 자동으로 여러 인스턴스로 늘리면 참가자가 서로 다른 서버로 접속할 수 있습니다.

같은 Windows 게임 폴더에서 창 없이 서버만 실행하려면 프로젝트의 `start-online-server.cmd`를 사용합니다. 이미 8787 서버가 실행 중이면 새 서버를 열지 말고 그 주소에 접속하세요.

## 경기 규칙과 연결 종료

- 방장은 대기실에서 5개 맵 중 하나를 선택합니다. 맵 변경 시 다른 참가자의 준비 상태가 해제됩니다.
- 최소 2명과 전원 준비가 필요하며, 최대 8명입니다. 경기 중 참가와 관전 참가는 지원하지 않습니다.
- 서버가 60회/초 주행·드리프트·부스터·충돌·복귀·랩 기록을 계산하고 20회/초 상태를 보냅니다. 클라이언트가 임의 위치나 부스터 개수를 제출할 수 없습니다.
- Esc는 자신의 조작 메뉴만 엽니다. 온라인 경기는 멈추지 않습니다. 창을 벗어나면 입력을 해제하고, 입력이 끊기면 서버도 가속을 중단합니다.
- 첫 완주 후 나머지 참가자에게 45초를 줍니다. 전체 경기는 최대 10분입니다. 시간 내 완주하지 못하면 미완주로 표시합니다.
- 결과는 완주 여부, 완주 시간, 진행 거리 순입니다. 방장이 결과 화면에서 대기실로 돌아가면 다음 경기를 준비합니다.
- 접속 종료자는 경기에서 빠지고 대기실 방장은 자동으로 승계됩니다. 끊긴 경기 중간에 같은 자리로 재접속하는 기능은 아직 없습니다.
- 초대 코드 기반 게스트 플레이입니다. 계정·친구 목록·공개 매칭·채팅·영구 기록 서버·운영 관리 시스템은 포함하지 않습니다.

## 이번 버전 확인 범위

변경된 멀티플레이 규칙과 실제 로컬 WebSocket 연결을 검사합니다: `npm run test:online`.
공개 호스팅, 서로 다른 가정의 인터넷 환경, WAN 지연 부하 테스트는 서버가 개설된 뒤 별도 확인이 필요합니다.

구현 참고 문서: [ws](https://github.com/websockets/ws), [Electron WebPreferences](https://www.electronjs.org/docs/latest/api/structures/web-preferences).
