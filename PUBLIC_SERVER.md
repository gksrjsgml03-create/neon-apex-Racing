# 다른 집에서 함께 플레이하기

## 현재: 무료 인터넷 체험 서버

V1.6.0의 **멀티플레이 → 인터넷 친구와 방 만들기**를 누르세요. 만들어진 방의 초대 정보를 친구에게 보내면 같은 공유기에 있지 않아도 참가할 수 있습니다. 친구도 V1.6.0을 사용하는 것을 권장합니다.

현재는 운영자 PC의 게임 서버를 Cloudflare Quick Tunnel로 연결한 **임시 체험 서버**입니다.

- 참가자는 별도 Cloudflare 계정이나 도구 설치 없이 게임만 실행합니다.
- 운영자 PC와 게임 서버·터널 프로세스가 켜져 있어야 합니다. 절전·재부팅·네트워크 단절 시 접속이 끊깁니다.
- 게임 창을 닫아도 별도로 실행한 체험 서버는 계속 동작할 수 있습니다.
- 터널을 새로 시작하면 임시 주소가 바뀝니다. 고정 주소나 가용성을 보장하지 않습니다.
- 포트 포워딩이나 방화벽 설정을 자동 변경하지 않습니다. 터널은 게임 전용 서버의 18787 포트만 연결하며 파일 서버를 공개하지 않습니다.
- 공개 주소에는 초대 방만 있습니다. 계정 인증·공개 매칭·상용 운영 보호를 갖춘 서비스는 아닙니다.

서버 주소는 `public-server.json`에 기록됩니다. 앱은 시작할 때 동봉된 설정을 사용하고, 프로젝트 GitHub의 같은 파일을 한 번 확인해 주소 갱신을 반영합니다. 접속이 안 되면 운영자에게 체험 서버 실행 상태를 확인하세요.

Cloudflare도 Quick Tunnel을 개발·시험용으로 안내합니다. 정식 상시 운영 서버가 아닙니다. [공식 설명](https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/do-more-with-tunnels/trycloudflare/)

### 운영자 PC에서 상태 확인 / 중지

프로젝트 폴더의 PowerShell에서:

```powershell
.\scripts\public-demo.ps1 -Action Status
.\scripts\public-demo.ps1 -Action Stop
```

다시 시작하려면 `-Action Start`를 사용합니다. 프로젝트에 설치된 Node.js와 `.runtime/cloudflared.exe`가 필요합니다. 검증한 도구는 Cloudflare 공식 2026.9.1 Windows x64 배포 파일입니다. 프로그램·로그·프로세스 정보는 `.runtime/`에만 보관하고 Git과 게임 ZIP에서 제외합니다.

새 주소는 `.runtime/tunnel.err.log`에 나타납니다. `public-server.json`의 endpoint를 `wss://새주소`로 바꾸고 GitHub에 반영해야 앱의 기본 주소도 갱신됩니다. 임시 URL이 바뀌면 예전 초대 정보도 다시 보내야 합니다. 상태/중지는 이 스크립트가 만든 프로세스의 경로·시작 시각까지 확인하고 수행합니다.

## 다음 단계: PC를 꺼도 운영되는 무료 호스팅

[Render 무료 서버 배포 시작](https://render.com/deploy?repo=https://github.com/gksrjsgml03-create/neon-apex-Racing)

`render.yaml`에 Singapore 지역의 **Free** 게임 서버 1개를 준비했습니다. 계정 생성·로그인·이용약관 동의는 소유자가 직접 진행해야 하며 현재 Render 서버를 개설하거나 결제하지 않았습니다.

1. 위 링크에서 본인 계정으로 로그인합니다.
2. 배포 대상이 이 저장소인지, 서버 요금제가 **Free**인지 확인합니다. 유료 옵션을 선택할 필요가 없습니다.
3. 서버를 배포하고 제공된 HTTPS 주소의 `/health`에서 `ok: true`를 확인합니다.
4. 주소를 게임 서버 입력란에 넣습니다. 게임은 HTTPS 주소를 WSS 주소로 바꿔 연결합니다.
5. 앱의 기본 서버로 쓰려면 `public-server.json`에 WSS 주소, `kind: "hosted"`, 안내 문구를 반영합니다.

Render Free는 일정 시간 활동이 없으면 쉬고, 다시 연결할 때 기동 시간이 걸릴 수 있습니다. 첫 접속이 실패하면 `/health`가 응답할 때까지 기다린 후 다시 참가하세요. 무료 서비스는 사용량 제한이 있으며 요금제·정책은 서비스 화면에서 확인해야 합니다. [무료 서비스 안내](https://render.com/docs/free) · [WebSocket 지원](https://render.com/docs/websocket)

방 상태는 메모리에 보관하므로 재배포나 서버 재시작 시 방은 사라집니다. 현재는 서버 인스턴스 1개로 운영해야 합니다. 상용 운영으로 확장할 때 계정, 재접속, 모니터링, 방 데이터 관리와 서버 용량을 별도로 설계해야 합니다.

## 상대 카트 움직임 개선

- 서버 전송 간격은 20Hz로 유지하고 상대 위치를 약 **100ms** 모아 프레임마다 보간합니다.
- 위치·조향·기울기·점프 높이를 연결해 그립니다. 데이터 도착 간격이 흔들려도 재생 시각을 천천히 보정합니다.
- 데이터가 잠시 늦으면 최대 **100ms**만 추가 예측하고 이후에는 더 멀리 달려가지 않도록 제한합니다.
- R 복귀는 새 위치로 전환하고 이전 위치부터 길게 미끄러져 이동시키지 않습니다.
- 본인 카트의 작은 서버 위치 보정도 표시 단계에서 부드럽게 줄입니다. 충돌·부스터·랩 판정은 여전히 서버 상태를 사용합니다.
- 긴 지연에는 ‘연결 지연’을 표시합니다. 표시가 부드러워지는 만큼 상대 차량은 약간 과거 위치로 보이며, 큰 네트워크 단절까지 없애지는 못합니다.

검증: 위치 사이 보간, 지연·예측 제한, 복귀, 도착 간격 변화, 본인 위치 보정과 실제 공개 WSS의 두 참가자 접속을 확인했습니다. 서로 다른 가정의 회선이나 다수 플레이어의 장시간 부하 테스트를 완료했다는 의미는 아닙니다.
