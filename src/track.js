// Compatibility exports for the original single-course rules tests.
export { SEGMENT, segmentAt } from './tracks.js';
import { TRACKS } from './tracks.js';
export const TRACK=TRACKS[0].segments;
export const LENGTH=TRACKS[0].length;
